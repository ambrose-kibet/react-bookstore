const Categories = () => (
  <section className="section-container">
    <button className="btn status-btn btn-primary" type="button">
      Check status
    </button>
  </section>
);
export default Categories;
